# CrochetIQ deck: PowerPoint 2019 compatibility + real-life example

Two changes to the existing 6-slide deck. No app source files change.

## 1. Make the file open in PowerPoint 2019

The current file was generated by pptxgenjs and needs a compatibility pass. Fix approach:

- Regenerate the deck, then run it through LibreOffice in headless mode to re-save it as a clean, strictly conformant `.pptx` (Impress writes an OOXML package that PowerPoint 2016/2019 accepts).
- Re-run schema validation with auto-repair on the final file, and confirm the package opens and renders (convert to PDF and inspect every slide again).
- Keep images as embedded JPEG/PNG at moderate resolution so the file stays a reasonable size for older PowerPoint.
- Deliver as a new file, `CrochetIQ-Overview-v2.pptx`, so the original stays available.

## 2. Use the uploaded keychain photo as the real-life example

- Run the uploaded marigold-flower crochet keychain photo through CrochetIQ end-to-end in a live guest run against the running app: upload -> goal selection -> AI processing -> results.
- Capture the results screen with the real AI output (enhanced photo, analysis, captions, pricing in INR, inspiration ideas) plus the enhanced image itself.
- Replace only the example imagery/content on the results slide (slide 5). Other slides keep their current screenshots and copy.
- If the live run cannot complete (guest quota or AI limits), the slide keeps whatever the run did produce and I will tell you exactly what was missing rather than fabricating output.

## Technical notes

Screenshots come from headless Playwright at 1440 width against the local dev server. The deck is rebuilt with pptxgenjs (16:9, base64-embedded images), then round-tripped through LibreOffice for legacy PowerPoint compatibility, validated, rendered to images, and every slide inspected for overflow/overlap before delivery.
